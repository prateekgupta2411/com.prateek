package Searching;public class Linear_Search_In_Array {    public static void main(String[] args) {        int[] arr = {2, 4, 20, 25, 30};        System.out.println(search(arr, 250));    }    // Search in the array: return the index if item is found    //otherwise if item is not found return -1    static int search(int[] arr, int item) {        if (arr.length == 0) {            return -1;        }        // run a for loop        for (int i = 0; i < arr.length; i++) {            // check for element at every index if it is = item            if (arr[i] == item) {                return i;            }            //  this line will execute if none of the return statements above have executed            //  hence the target not found        }        return -1;    }}